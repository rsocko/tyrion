name: Publication disabled

on:
  workflow_dispatch:

permissions: {}

jobs:
  publication-disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Publication remains fail-closed
        run: echo "Publication is disabled pending independent infrastructure assurance."
